// @errata/graph — Bolt client + Cypher builders + ids (blake2b) + subset linter.
import { version as core } from '@errata/core';

export const version = '0.0.0';
export const dependsOn = { core } as const;
