# OpenRouter (LLM) — required for extraction/judge/eval; leave empty until funded
OPENROUTER_API_KEY=
ERRATA_BUDGET_CAP=50
# HydraDB (Bolt) — bolt:// NEVER neo4j://
HYDRA_BOLT_URL=bolt://127.0.0.1:7687
HYDRA_TOKEN=
# demo default history id (a knowledge-update: pre-approval $350,000 → $400,000)
ERRATA_DEMO_HISTORY=852ce960
# MinIO (local stack)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
